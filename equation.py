# This code calculates the area of a rectangle and then prints it
length = 15
width = 8
area = length * width

print("The area of the rectangle has a length of " + str(length) +
      " and a width of " + str(width) + " is " + str(area))
